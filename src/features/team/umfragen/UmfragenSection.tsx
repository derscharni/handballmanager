import type { TeamSectionProps } from '../../props'

// Platzhalter — wird durch die Feature-Implementierung ersetzt.
export default function UmfragenSection(_props: TeamSectionProps) {
  return (
    <div className="flex h-[30dvh] items-center justify-center text-muted font-display uppercase tracking-wide">
      UmfragenSection — in Arbeit
    </div>
  )
}
